# URL Shortener

A serverless URL shortener built with AWS Lambda, DynamoDB, API Gateway, and React.

## Stack
- **Backend**: AWS Lambda (Python), DynamoDB, API Gateway
- **Frontend**: React + TypeScript, hosted on S3 + CloudFront
- **IaC**: Terraform